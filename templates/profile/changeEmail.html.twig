{% extends 'base.html.twig' %}

{% block body %}
    <h1>Zmień email</h1>

    {{ form_start(form) }}
        {{ form_row(form.newEmail) }}
        {{ form_row(form.oldEmail) }}
        {{ form_row(form.submit) }}
    {{ form_end(form) }}
{% endblock %}
